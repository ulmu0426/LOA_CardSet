<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lostarkcardstatus">

    <application
        android:allowBackup="true"
        android:icon="@drawable/loading"
        android:label="LOA 카드 매니저"
        android:roundIcon="@drawable/loading"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:hardwareAccelerated="true"
        android:theme="@style/Theme.LostArkCardStatus">
        <activity
            android:name=".LoadingActivity"
            android:theme="@style/SplashTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CardSetPage"></activity>
        <activity android:name=".CardBookPage"></activity>
        <activity android:name=".DemonExtraDmgPage"></activity>
        <activity android:name=".SettingCard"></activity>
        <activity android:name=".Test"></activity>
        <activity android:name=".MainPage"></activity>

    </application>

</manifest>